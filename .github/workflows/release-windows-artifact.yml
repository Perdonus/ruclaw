name: Windows Release Artifact

on:
  push:
    branches: ["main"]
    tags: ["v*"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  release-windows:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Setup Go
        id: setup-go
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm
          cache-dependency-path: web/frontend/pnpm-lock.yaml

      - name: Setup pnpm
        run: corepack enable && corepack prepare pnpm@latest --activate

      - name: Build Windows release archive
        env:
          GOVERSION: ${{ steps.setup-go.outputs.go-version }}
        run: ./scripts/build-release-artifact.sh windows

      - name: Upload Windows release artifact
        uses: actions/upload-artifact@v4
        with:
          name: ruclaw-windows-release-${{ github.sha }}
          path: |
            release-artifacts/ruclaw_Windows_x86_64.zip
            release-artifacts/ruclaw_Windows_x86_64.zip.sha256
          if-no-files-found: error
          retention-days: 14
